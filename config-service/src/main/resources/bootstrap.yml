server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NicolasOrtiz7/ms-prueba.git
          searchPaths: config-data
  application:
    name: config-service